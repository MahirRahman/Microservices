<%- include('header'); %>
<body>
    <div class="container my-5">
        <h1> URL Shortener </h1>
        <form action="/shortUrls" method="post" class="form-inline my-5">
            <label class="sr-only" for="id_url" >Original URL</label>
            <input type="url" class="form-control col mb-4 mr-2" name="id_url" id="id_url" placeholder="Your url goes here">
            <button type="submit" class="btn btn-success mb-4">Submit</button>
        </form>
        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th> Original URL</th>
                    <th> Short URL </th>
                    <th> Clicks </th>
                </tr>
            </thead>
            <tbody>
                <% shortUrls.forEach(shortUrl => { %>
                    <tr>
                        <td>
                            <a href="<%= shortUrl.full %>"> <%= shortUrl.full %> </a>
                        </td>
                        <td>
                            <a href="<%= shortUrl.short %>"> <%= shortUrl.short %> </a>
                        </td>
                        <td><%= shortUrl.clicks %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>